# Log — 2026-03-05

## Cambios realizados

- Se creó la estructura de documentación del proyecto:
  - `docs/specs/`
  - `docs/work_orders/`
  - `docs/checklists/`
  - `docs/logs/`
- Se consolidaron las especificaciones oficiales en `docs/specs/` (00 a 09).
- Se creó `README_SPECS.md` para definir la fuente de verdad y orden de lectura.
- Se creó `docs/work_orders/WORK_ORDER_AUTH.md` como contrato del bloque activo.
- Se creó `docs/checklists/DOD_AUTH.md` con DoD del módulo de autenticación.

## Bloque activo

- AUTH: Login / Forgot Password / Logout / Router Guards / FCM token post-login.

## Actualización — Plan BACKEND_ROLES

- Se creó `docs/work_orders/PLAN_BACKEND_ROLES.md` con plan por días (Día 1 a Día 5 buffer).
- Se registró secuencia de entregables, criterios Done, riesgos/mitigaciones y commits esperados.
- Se confirma que el bloque activo operativo para backend es: **BACKEND_ROLES**.

## Actualización — Reestructuración del plan BACKEND_ROLES

- Se reestructuró `docs/work_orders/PLAN_BACKEND_ROLES.md` a formato operativo por día.
- Cada día quedó con 4 actividades (cumple rango 4–8 actividades/día).
- Cada actividad ahora incluye: objetivo, archivos exactos, evidencia de done, riesgo+mitigación y commit propuesto.
- Se agregó regla obligatoria de Git por actividad: `git add -A` + `git commit` + `git push` + reporte breve al usuario.
- Se mantiene alcance exclusivo BACKEND_ROLES (functions + rules + indexes + README + evidencias).
- Micro-ajustes aplicados: allowlist de superadmins definida por secrets/params con fail-closed; creación de `admins/{uid}` con `merge: true` y payload mínimo; en Día 3 rules/indexes quedan solo versionados y el deploy se documenta/ejecuta en Día 4.

## Actualización — Implementación BACKEND_ROLES completada

- Implementado `functions/src/config/superadmins.ts` con params `SUPERADMIN_UIDS` y `SUPERADMIN_EMAILS` (fail-closed).
- Implementado `functions/src/utils/authz.ts` con validación de superadmin y rechazo por defecto.
- Implementado `functions/src/auth/on_auth_user_create.ts` (1st gen): claim `patient` + `patients/{uid}` mínimo.
- Implementado `functions/src/auth/set_admin_role.ts` (callable segura): auth, allowlist, bloqueo auto-promoción, idempotencia y upsert de `admins/{uid}`.
- Implementado `functions/src/auth/set_fcm_token.ts` (callable segura): auth + role claim + validación de token + merge por colección.
- Versionados `firestore.rules` y `firestore.indexes.json` (7 índices).
- Creado `functions/README.md` con prechecks, deploy, verificación de claims y pruebas manuales mínimas.
- Verificación técnica local: `npm run build` en `functions/` sin errores.
- Intento de deploy real: bloqueado por autenticación Firebase CLI en VM (`Failed to authenticate, have you run firebase login?`).
- Se actualizó `firebase.json` para incluir targets `functions` y `firestore` y permitir despliegue completo cuando se autentique la CLI.
- `WORK_ORDER_BACKEND_ROLES.md` actualizado con estado: implementación completada y deploy pendiente por auth.
