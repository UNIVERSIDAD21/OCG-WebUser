# Log técnico — 2026-03-09

## Proyecto
OCG Clínica (`OCG-WebUser/ocg_proyect`)

## Bloque trabajado
**BLOQUE_04 — Gestión de Pacientes (reabierto)**

## Cambios implementados

1. **Baseline técnico limpio**
   - Corrección de issues de `flutter analyze` (imports, deprecaciones `withOpacity`, contexto async)
   - Estado base: `flutter analyze` ✅, `flutter test` ✅

2. **Modelo de paciente actualizado**
   - Se agregó `proximaCita` a `PatientModel`
   - Serialización/deserialización de `proximaCita`
   - Pruebas nuevas en `test/patient_model_test.dart`

3. **Tarjeta de paciente (admin)**
   - Soporte de foto de perfil (`fotoUrl`) con fallback a iniciales
   - Visualización de próxima cita o “Sin cita programada”

4. **Denormalización de próxima cita**
   - `AppointmentsRepository` recalcula y actualiza `patients/{id}.proximaCita` al:
     - crear cita
     - actualizar estado de cita
     - reprogramar cita

5. **Flujo admin de alta corregido**
   - FAB en `AdminPatientsScreen` ahora muestra diálogo de instrucción de flujo real
   - Se añadió filtro `Pendientes`

6. **Formulario de paciente corregido**
   - `PatientFormScreen` elimina creación manual con UID en modo creación
   - Modo creación ahora muestra instrucción y redirige al flujo correcto

7. **Perfil del paciente habilitado para edición permitida**
   - Edición de teléfono
   - Subida de foto de perfil (`image_picker` + `firebase_storage`)
   - Acción de cambio de contraseña (`resetPassword`)
   - Campos clínicos permanecen en solo lectura (con candado)

8. **Placeholders de tabs reemplazados**
   - `patient_payments_tab.dart` y `patient_simulator_tab.dart` migrados a `OcgEmptyState`

## Verificación final
- `flutter analyze` ✅
- `flutter test` ✅

## Estado
Bloque 04 implementado técnicamente y **listo para validación funcional de Jefe** antes de marcar cierre definitivo en índice maestro.
