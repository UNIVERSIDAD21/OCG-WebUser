{
  "name": "ocg-functions",
  "private": true,
  "engines": {
    "node": "20"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf lib",
    "lint": "echo 'lint pendiente en actividad posterior'",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "npm run build && firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "firebase-functions": "^6.0.1",
    "openai": "^4.104.0",
    "pdfkit": "^0.18.0"
  },
  "devDependencies": {
    "@types/pdfkit": "^0.17.6",
    "typescript": "^5.9.2"
  }
}
